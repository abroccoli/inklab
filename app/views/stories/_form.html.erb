<%= form_for [@story] do |f| %>

    <%= f.label :title %>
    <%= f.text_area :title %>

    <%= f.fields_for :lines, @story.lines.new do |line_f| %>
      Line content: <%= line_f.text_field :content %>
    <% end %>

    <%= f.submit 'Create Story' %>
<% end %>
