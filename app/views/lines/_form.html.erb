<%= form_for [@story, @line] do |f| %>

    <%= f.label 'enter new line:', class: 'form_label'%>
    <br>
    <%= f.text_area :content, class: 'line_entry' %>
    <br>
    <%= f.submit 'Add Line', class: 'btn btn-black' %>
<% end %>
`
